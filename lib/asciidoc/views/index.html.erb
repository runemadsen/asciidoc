<div id="index">
	<h2>Index</h2>
	
	<ul>
	<% cur_letter = "0" %>
	<% hash = filter_results[:indexfilter] %>
	<% keys = hash.keys.sort {|x,y| x.downcase <=> y.downcase } %>

	<% keys.each do |key| %>
		
		<% if key[0].downcase != cur_letter.downcase %>
			<% cur_letter = key[0].upcase %>
			<li class="big-letter"><%= cur_letter %></li>
		<% end %>

		<li>
			<span class="index-term-name"><%= hash[key][:elements].first.render_children(views, filter_results) %></span>
		
			<% hash[key][:elements].each_with_index do |el, i| %>
				<a href="#<%= el.link_id %>" class="index-term-link">&nbsp;</a><% if i < hash[key][:elements].size - 1 %>,<% end %>
			<% end %>	

			<% unless hash[key][:children].empty? %>
				<% child_hash = hash[key][:children] %>
				<% child_keys = child_hash.keys.sort %>
				<ul>
					<% child_keys.each do |child_key| %>
						<li>
							<%= child_key %>
							<% child_hash[child_key][:elements].each_with_index do |child_el, j| %>
								<a href="#<%= child_el.link_id %>" class="index-term-link">&nbsp;</a><% if j < child_hash[child_key][:elements].size - 1 %>,<% end %>
							<% end %>
						</li>
					<% end %>
				</ul>
			<% end %>
		</li>

	<% end %>
	</ul>

	<%#= filter_results[:indexfilter].inspect %>
	
</div>